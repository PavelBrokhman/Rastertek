using RastertekCS.Windows.Tutorial23.System;

namespace RastertekCS.Windows.Tutorial23;

internal static class Program
{
    private static int Main()
    {
        var system = new SystemFramework();
        if (system.Initialize())
        {
            system.Run();
        }
        Environment.Exit(0);
        return 0;
    }
}
